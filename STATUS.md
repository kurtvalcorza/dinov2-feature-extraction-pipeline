# Release status

Current status: **Candidate** — tutorial source complete, awaiting clean-runtime execution. The pipeline package with its adaptation contract (`features`, `knn_baseline`, `adapt` with the frozen / unfrozen policies, `evaluate`, `classify`, `save_artifact` / `from_artifact`; the pinned iNaturalist photo table and BYOD loaders in `samples.py`; accuracy, macro-F1, the majority floor and the k-NN baseline in `metrics.py`), offline unit tests (35, no weights required, plus 5 notebook-parity tests and 2 model-backed tests that run when the snapshot is staged), a local CPU execution of the committed notebook blob in the Windows venv (k-NN 83.3 % → linear probe 83.3 % → selected policy `unfrozen last 2 blocks + linear head` 83.3 % on 48 held-out photographs against a majority floor of 16.7 %; recorded in `docs/release-verification.md`), `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the standalone `E2E` tutorial notebook `tutorials/dinov2_feature_extraction_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) with its static validator, generator parity checks and CI workflow exist. Unit tests and static conformance checks are expected to run in GitHub Actions. The earlier `TASK-INFERENCE` notebook's Kaggle CPU run (blob `46d155ab2f9a`, commit `347e21d`) is retained as history and is not evidence for the `E2E` blob. The tutorial remains non-release-grade until clean-runtime execution evidence for the exact candidate commit is recorded according to `docs/release-verification.md`.
